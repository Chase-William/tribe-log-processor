{
  "name": "tribe-log-processor",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "init": "yarn && yarn run build",
    "build": "tsc --build",
    "test": "yarn build && node test.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "xregexp": "^5.1.0"
  },
  "devDependencies": {
    "@types/node": "^16.9.2",
    "fs": "^0.0.1-security",
    "typescript": "^4.4.3"
  }
}
